class UserModel {
  final String? uid;
  final String? name;
  final String? email;
  final String? imageURL;

  // User(this.uid, this.name, this.email, this.imageURL);
  UserModel({
    required this.uid,
    required this.name,
    required this.email,
    required this.imageURL,
  });
}
